# [UX-03] Run CSV Export on Background Thread in `QuantLabViewModel`

**Status:** ✅ Complete  
**Category:** UI/UX  
**Size:** XS  

## Summary
Replaced synchronous `ExportCsv()` in `QuantLabViewModel.vb` with `ExportCsvAsync()`. `SaveFileDialog` still opens on the UI thread; CSV content is built on the UI thread via extracted `BuildCsvContent()` helper; `File.WriteAllText` is offloaded to `Task.Run`. Renamed local variable `path` → `filePath` to avoid VB.NET case-insensitive clash with `System.IO.Path`. Build: 338 passed, 0 failed.
